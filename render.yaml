services:
  - type: web
    name: canva-dining-bot
    runtime: docker
    plan: free
    dockerContext: .
    dockerfilePath: Dockerfile
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: CHANNEL_ID
        value: "1234567890"
      - key: PORT
        value: "8080"
